# Environment Variables Configuration
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=3000
NODE_ENV=development

# Email Configuration (Required for contact form)
# For Gmail, you'll need to use an App Password (not your regular password)
# Go to: Google Account Settings > Security > 2-Step Verification > App passwords
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Admin email (where contact form submissions will be sent)
ADMIN_EMAIL=admin@refillplanet.com

# Production Configuration
# Uncomment and update these for production deployment
# NODE_ENV=production
# PORT=443

# Alternative Email Services
# For other email services, update EMAIL_SERVICE accordingly:
# EMAIL_SERVICE=outlook
# EMAIL_SERVICE=yahoo
# EMAIL_SERVICE=zoho

# Custom SMTP Configuration (if not using a predefined service)
# SMTP_HOST=your-smtp-host.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Security (for production)
# SESSION_SECRET=your-super-secret-key-here
# JWT_SECRET=your-jwt-secret-key-here

# Database (if you plan to add one later)
# DATABASE_URL=mongodb://localhost:27017/refillplanet
# or
# DATABASE_URL=postgres://user:password@localhost:5432/refillplanet